# Homebrew Tap for Gloomberb

Install the macOS app and terminal command with:

```sh
brew install --cask vincelwt/tap/gloomberb
```

The Gloomberb release workflow updates `Casks/gloomberb.rb` when a new GitHub release publishes the macOS app archive.
